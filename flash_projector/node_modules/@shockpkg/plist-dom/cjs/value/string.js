"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ValueString = void 0;
var _options = require("../options.js");
var _util = require("../util.js");
var _value = require("../value.js");
/**
 * ValueString object.
 */
class ValueString extends _value.Value {
  /**
   * Value type.
   */
  static TYPE = 'string';

  /**
   * Tag names.
   */
  static TAG_NAMES = ['string'];

  /**
   * Value value.
   */
  value = '';

  /**
   * ValueString constructor.
   *
   * @param value The value.
   */
  constructor(value = '') {
    super();
    this.value = value;
  }

  /**
   * @inheritdoc
   */
  fromXmlElement(element) {
    (0, _util.assertXmlTagName)(element, 'string');
    this.value = (0, _util.xmlElementText)(element)?.nodeValue || '';
  }

  /**
   * @inheritdoc
   */
  toXml(options = null, depth = 0) {
    const p = (options?.indentString ?? _options.INDENT_STRING).repeat(depth);
    const e = this.value.replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;');
    return `${p}<string>${e}</string>`;
  }
}
exports.ValueString = ValueString;
//# sourceMappingURL=string.js.map